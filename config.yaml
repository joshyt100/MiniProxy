cleartext:
  enabled: true
  listen_addr: ":8080"

algo: rr

upstreams:
  # - http://demo-upstream1:80
  # - http://demo-upstream2:80
  - https://localhost:50051
  # - http://localhost:9001
  - http://localhost:50052

tls:
  enabled: true
  listen_addr: ":8443"
  cert: "certs/cert.pem"
  key: "certs/key.pem"

metrics:
  enabled: true
  listen_addr: ":2112"

rate_limit:
  enabled: false
  rps: 10
  burst: 5
  per_ip: true

health:
  enabled: false
